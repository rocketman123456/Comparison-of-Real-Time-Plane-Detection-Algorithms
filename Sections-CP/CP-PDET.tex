\documentclass[main.tex]{subfiles}

%% PLANE DETECTION %% 

\begin{document}

\section[Plane Detection]{Plane Detection}
SLAM returns OUTPUT, thus we have these options ... [explain] ... we choose these because
% 1. $SLAM gibt $OUTPUT aus, daher haben wir optionen an Algorithmen
% 2. [list PDA options w.r.t $REQ and $OUTPUT]
% 3. we have chosen these N PDAs because

\end{document}